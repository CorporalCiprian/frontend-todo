name: Prepare and build node

on:
  pull_request:
    branches: [main]

jobs:
  build_node:
    uses: CorporalCiprian/todo-template/.github/workflows/node/build-and-test.yml@main
    with: 
      language: 'node'
      version: '24'