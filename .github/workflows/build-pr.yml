name: Prepare and build node

on:
  pull_request:
    branches: [main]

jobs:
  build_node:
    uses: CorporalCiprian/todo-template/.github/workflows/build-template.yml@main
    with: 
      language: 'node'
      version: '20'